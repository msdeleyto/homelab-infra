[global]
keyboard = "es"
country = "es"
fqdn = "{{ item }}.homelab"
mailto = "{{ pve.email }}"
timezone = "Europe/Madrid"
root-password = "{{ pve.root_password }}"
root-ssh-keys = ["{{ pve_ssh_key }}"]
reboot-mode = "power-off"

[network]
source = "from-dhcp"

[disk-setup]
filesystem = "ext4"
disk-list = ["vda"]
